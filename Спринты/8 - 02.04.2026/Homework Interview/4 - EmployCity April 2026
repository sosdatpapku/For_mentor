#Задача №1 (SQL)
Есть левая таблица на 2 строки и правая на 3 строки:
1) inner join - макс 6 строк, мин 0
2) left join - макс 6 строк, мин 2
3) В случае когда в левой и правой таблицах все одинаковые строки

#Задача №2 (SQL)
from
join
where
group by
having
select
order by
limit / offset

#Задача №3 (SQL)
where rn = 1
Напрямую в WHERE использовать оконную функцию нельзя, потому что WHERE логически отрабатывает раньше вычисления оконных функций

#Задача №4 (SQL)
1) Вешать индекс логичнее всего на company_id, т.к по нему будем джойнить
2) Можно сделать составной индекс (company_id, branch_id)

#Задача №5 (Python)
1) pd.read_csv('path')
2) pd.to_csv('name')
3) df.isna().sum()
4) df.fillna(), df.dropna()

#Задача №6 (Python)

def func():
    pass # return

"return" в функциях не обязателен, она может возврщать None 

#Задача №7 (Python)
1) Поиск в set (O(1)) быстрее чем в list (O(n))
set реализован как хэш-таблица

#Задача №8 (Статистика)
1) Правильно будет ограничить наблюдения по n% перцентилю

#Задача №9 (Статистика)
Среднее - когда распределение симметрично
Медиана - когда распределение скошено или есть выбросы
Мода - для категориальных признаков

#Задача №10 (AB)

1) Формулируем гипотезу - определяем сегемент эксперимента
2) Выбираем целевую, защитные, информационные метрики
3) Выбираем ед.рандомизации, изучаем наличие сетевого эффекта
4) На исторических данных оцениваем дисперсию метрик, возможный MDE
5) Опеределяем длительность теста (размер выборок), учитываем сезонность внутри периода
6) Запуск теста
7) Мониторинг защитных метрик
8) Получение результатов, итоговая проверка корректности теста (SRM), подведение итогов и принятие решения
9) Документация эксперимента

#Задача №11 (AB)
1) Проверка корректности эксперимента
2) Если корректен, и значение метрики шло в нужную сторону, но p-value немного не дошел до alpha, то можно предложить перезапустить тест с большей длительностью,
    если новый MDE перекрывает затраты на эксперимент
3) Иначе просто заканчиваем эксперимент без перезапуска

#Задача №12 (AB)

Когда измеряем и проверяем множество метрик / групп есть вероятность ошибки 1 рода, возможно сегмент прокрасился случайно и использовать поправки на множественное сравнение
    Но следуется изучить этот сегмент - возможно это поле дял новых гипотез
    
    
#Задача №13 (Кейс)

1) Создать форму для запросов, ограничить формат ввода нужных полей
2) Выделяем значения полей
3) Строим автоматически вычисляемый отчёт
4) Подставлять значения полей в шаблон запроса или ETL-процесс / фильтры для отчёта
5) Возвращать отчёт с примененными параметрами в запрос на выгрузку

